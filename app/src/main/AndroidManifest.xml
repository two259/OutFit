<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.outfit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OutFit">
        <activity android:name=".MessageActivity" android:label="Chat" ></activity>
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".ProfileActivity" android:label="Profile"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".SocialActivity" android:label="Chat"/>
        <activity android:name=".HomeActivity" />
        <activity android:name=".CompetitionsInActivity" android:label="Competitions"/>
        <activity android:name=".CreateCompActivity" android:label="New Competition"/>
        <activity android:name=".SearchCompActivity" />
        <activity android:name=".CompCreateJoinSuccessActivity" />
        <activity android:name=".JoinCompetitionActivity" />
        <activity android:name=".CompetitionOverviewActivity" />
        <activity android:name=".CompeteActivity" />
        <activity android:name=".FullLeaderboardActivity" android:label="Score board"/>
    </application>

</manifest>