<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.outfit">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OutFit">
        <activity android:name=".EditProfileActivity"></activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".SocialActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".CompetitionsInActivity" />
        <activity android:name=".CreateCompActivity" />
        <activity android:name=".SearchCompActivity" />
        <activity android:name=".CompCreateJoinSuccessActivity" />
        <activity android:name=".JoinCompetitionActivity" />
        <activity android:name=".CompetitionOverviewActivity" />
        <activity android:name=".CompeteActivity" />
        <activity android:name=".FullLeaderboardActivity" />
    </application>

</manifest>